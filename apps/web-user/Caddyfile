# Serve the built SPA with a fallback to index.html for client-side routing.
# Railway injects $PORT; default 8080 for local runs.
:{$PORT:8080} {
	root * /srv
	encode gzip zstd
	try_files {path} /index.html
	file_server
}
